packages: .

-- Optional: Use this to keep all builds local to the project
-- so you don't pollute your global environment.
-- Comment out if you prefer shared builds.
write-ghc-environment-files: always

-- Optional: pin specific package versions
constraints:
  crypton < 1.0.7

-- Optional: suppress noisy output
-- verbosity: silent
